import { int, sqliteTable, text } from "drizzle-orm/sqlite-core";

export const notificationsTable = sqliteTable("notifications_table", {

})

export const prevDevicesTable = sqliteTable("prevDevices_table",{
    
})